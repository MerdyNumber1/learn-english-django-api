server {
    listen 0.0.0.0:${NGINX_PORT};

    location / {
        proxy_pass http://web_app:${APP_PORT};
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}
